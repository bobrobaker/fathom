"""Fathom diagnostic harness (dh) — spike package.

`schemas` is the single source of truth for the spec §3 data contracts;
everything else (generator, environment, controller, baselines, eval, viewer)
builds on those shapes.
"""
