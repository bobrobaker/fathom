# Fathom — tech-debt shelf

Project-local deferred work: refactors, architecture concerns, "fix later" items
spotted while editing. One row per item, append-only; check off when done, delete when
it stops mattering. Not the roadmap (planned work) or `docs/decisions/` (calls made) —
this is debt discovered mid-implementation.

**Capture trigger:** after a logical chunk, make one pass over the functions you just
edited and append anything deferred here, with enough locus to act on it cold.

**Row format:** `- [ ] path:symbol — observation (why deferred)`

## Shelf

<!-- e.g. - [ ] auth/session.py:refresh_token — re-derives the key every call (hot path); cache once the format stabilizes -->

- [x] src/dh/generator/faults.py:FAULTS — authored #4 (calibration_drift, change-is-cause), #6 (detector_bias_drift, absent-cue + buried evidence), #7 (tec variant, tie-breaker), #8 (common_mode_power). All 8 authored; anti-shortcut balance ≈0.33 (2 change-is-cause, 4 trigger≠cause, 1 absent-cue, 1 abstain), gated in test_capability.py.
- [x] src/dh/generator/compose.py:_build_corpus — replaced exclude_artifacts + the special-cased error tuple with one per-fault corpus **delta** (FaultFacts.corpus, CorpusItem). Shared structural corpus stays shared; conclusion-bearing items (window-clean result, reboot, error log) are per-fault facts each forward model declares (C1).
- [~] tools/run_eval.py — the ≥3-run variance report (S1) is **deferred by budget** (2026-06-21): n=3 over 8 cases burns the subscription allotment (see token-overhead item below). The **n=1 all-8-case set is done and assembled** in `reports/bespoke.md` with explicit "variance not characterized" + cost-undercount caveats. Upgrading to n=3 is gated on first fixing the per-call overhead. Earlier `--all --runs 3` attempts were paused mid-run (2026-06-16 usage limit; 2026-06-21 ×2 audit-driven kills).
- [ ] src/dh/checks.py:common_mode_check + controller interpret — **REGRESSION SIGNAL (verify live):** in the single A3 case1 run the controller scored acc=0 (wrong cause) though trig/conflict=1 (the sweep worked). The earlier pre-additions smoke got case1 right (acc=1). Captured evidence (the live propose-prompt) shows the LLM reading `common_mode=false` on case1's TEC **cascade** (staggered downstream onsets, spread≈4.25d) as "independent faults," crediting the laser/detector decoys and demoting TEC; compounded by 3 thermal hypotheses co-saturating at the +3.0 clamp (margin≈0 → no clean leader → synthesize mis-picks). The case8 checks (common_mode, detector_health) are cross-case confusers on case1. Fix candidates (need a live run to validate, so deferred): (a) common_mode_check distinguishes a causal *cascade* (one cause, lagged) from truly *independent* onsets rather than only "synchronous vs not"; (b) interpret prompt: a non-common-mode result does NOT imply independent faults (a cascade is also not common-mode); (c) reconsider seeding 5 overlapping thermal hypotheses. Re-measure case1 accuracy over ≥3 runs after any change (do NOT tune to manufacture a win, #5).
- [ ] src/dh/eval/benchmark.py — add tools/load_musique.py (the `benchmark` extra) and run the real n=20 fail-fast (A5). TIME-BOXED, not a ship-blocker; report-only if not competitive. Never ship the synthetic smoke (B9).
- [x] src/dh/controller/llm.py:interpret_result — A6 prompt iteration: SYSTEM_PROMPT + propose/interpret now push discriminating actions, negative links (contradict a decoy), and recording a demoted trigger / stuck channel in conflicts. Re-measure live (A3). B7 also handled structurally by the cumulative log-odds clamp (beliefs.MAX_LOG_ODDS).
- [x] src/dh/controller/llm.py:interpret_result — A7: evidence `source` action-key stripped to the check name and signals mapped to metric nodes, applied identically to all four solvers (test_scoring_symmetry.py). CHECK_IDS now matches the real check names (config_diff was mismatched before).
- [ ] src/dh/environment.py:_bm25_rank — plain BM25, no stemming; the `hybrid` extra (sentence-transformers) is the intended fix. Deferred (A9): lexical search is adequate at the spike's corpus volume; hybrid only matters at corpus-volume cases / the benchmark.
- [x] src/dh/environment.py:LidarEnvironment.query_telemetry — the `condition` arg now filters on a parallel `spec["conditions"]` tag list (graceful no-op when unpopulated); the full-build D3/D4 cases populate it.
- [x] src/dh/schemas.py:InvestigationGraph.snapshots — handled: loop._snapshot appends a snapshots-stripped deep copy (no O(n²) nesting).
- [ ] tools/run_eval.py (or new tools/ablate_voi.py) — VOI ablation: LLM-scalar vs entropy-over-predicted-outcomes vs random-order vs playbook-fixed, to measure whether VOI scoring buys anything at spike scale (5 hyps, ~6 cheap checks). `expected_discrimination` is an uncalibrated LLM scalar (schemas.py:216 default 0.5); cost is hardcoded (config.yaml:voi_costs); nothing scores VOI calibration. Blocks the formal-VOI build (docs/decisions/2026-06-16-voi-ablation-before-formalizing.md). Deferred: spike scale may not need it.
- [ ] src/dh/graph.py:to_networkx — networkx is a declared dep (pyproject) whose only user is `to_networkx`, which is called nowhere; `neighbors`/`traverse` scan `g.edges` directly (graph.py:18). Remove the dep + helper, or route traversal through it. Cleanup only — the scan is fine at 6-subsystem scale. (Verified 2026-06-16.)
- [ ] docs/handoff/ontology_typed_links.md + docs/handoff/worked_sut_example_lidar.md — terminology sweep: those docs label the authored `affects` graph with causal-formalism wording (run `tools/lint.py` for the exact strings); it is a confidence-weighted directed-influence graph (no structural equations / do-calculus). Rename to influence/coupling graph; keep `causal_chain` / "non-causal" (finding-level claims). Now WARNed by tools/lint.py:check_causal_terminology. (Note: CLAUDE.md does NOT contain the wording — the handoff's claim that it did was stale.)

<!-- 2026-06-21 eval-fairness & cost audit (see docs/decisions/2026-06-21-eval-fairness-and-scoring-audit.md) -->
- [ ] src/dh/controller/llm.py:CLIBackend.complete — **COST ROOT CAUSE: ~22.6k tokens of scaffolding per `claude -p` call** (measured via `--output-format json`: ~8k cache_creation + ~14.6k cache_read for a 3-token prompt, even with `--system-prompt`). It's the auto-discovered **global `~/.claude/CLAUDE.md`** + dynamic system sections (cwd/env/git/memory). Scratch-cwd skips only the *project* CLAUDE.md. Model IS correctly sonnet — overhead, not model, drove ~40% of a session budget over ~300–400 calls. Fix options (none free of tradeoffs): `--exclude-dynamic-system-prompt-sections` (free, partial, keeps OAuth); skip global CLAUDE.md via `--setting-sources` excluding `user` or a scratch `$HOME` (test it doesn't break OAuth); `--bare` (eliminates scaffolding but forces ANTHROPIC_API_KEY → metered API, off subscription); lower `--budget` 8→4 to ~halve controller calls.
- [ ] src/dh/controller/llm.py:MeteredBackend — **undercounts real cost by ~15×.** Counts only our prompt (chars/4); never sees the ~22.6k/call scaffolding above. Its docstring claim "little hidden scaffolding remains, so this approximates API input tokens" is **FALSE** (verified against live `usage`). Fix: capture the CLI's actual `usage` (run with `--output-format json`, parse cache_creation/read/input/output) so the eval's cost column + A4 are honest. The current `tokens` column in reports is *relative content volume*, not billed cost — labelled as such in `reports/bespoke.md`.
- [ ] src/dh/controller/llm.py:interpret_result — **CONTROLLER WEAKNESS (Phase-2 shelf, report-as-is for the spike — user call 2026-06-21).** The interpret step over-credits **nominal/at-spec** check results as positive support, saturating a hypothesis to the +3.0 log-odds clamp (conf 0.953); since `tau_min=0.55` only needs log-odds >~0.2, this blocks abstention. Confirmed across the n=1 set: controller confabulates on case5 (abstain; react was the only correct abstainer) and over-reasons past both change-is-cause causes (case4, case8) and the tie-breaker (case7) → **accuracy 4/8, the worst of the three LLM solvers (bare_llm 6, react 5).** Same mechanism as the old case1 regression. Existing prompt already says "nominal argues against" and is insufficient. Candidate (general, NOT case-by-case — overfitting risk #5): deterministic non-positive link for any nominal check; re-measure all cases, win or lose. **Do not patch against these 8 cases.** Deferred to Phase 2; the spike reports the weakness honestly.
- [ ] docs/handoff/spike_spec.md §9 S1 — the ≥3-run variance exit criterion is **unmet by budget** (n=1 shipped). A4/findings must state this as a known limitation, not paper over it. Revisit when per-call overhead is fixed (cheaper runs make n=3 affordable).
