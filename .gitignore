# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Monition store: the binary Dolt working copy stays local; git tracks the
# reviewable text dump (monition/dump.sql), regenerated by the pre-commit hook.
monition/.dolt/

# Datasets / large fixtures pulled at runtime (MuSiQue etc.)
data/
*.parquet
